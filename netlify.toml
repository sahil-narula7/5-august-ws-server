[build]
  command = "bun install --frozen-lockfile && bun --cwd apps/frontend run build"
  publish = "apps/frontend/dist"

[build.environment]
  BUN_VERSION = "1.3.11"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
